extends ../layout

block main
  h1= title

  p #[strong Name:]
    p 
      a(href=coupon_detail.url) #{coupon_detail.name}
    p #{coupon_detail.description}
    p Discount: #{coupon_detail.discount_percent}%
    p Valid from #{coupon_detail.begin_date_formatted} 
    if  coupon_detail.expiration_date
      | til #{coupon_detail.expiration_date}
    section
      h2 Valid Groups
      ul
        each group in coupon_detail.valid_item_groups
          li 
            p #{group.name}
        else
          li There are no valid groups.
  hr
  p
    a(href=coupon_detail.url+'/delete') Delete Coupon
  p
    a(href=coupon_detail.url+'/update') Update Coupon